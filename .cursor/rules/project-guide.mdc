---
description: StudyBuddyAI 项目全局约束 — 所有代码修改必须遵守
globs: ["**/*.py", "**/*.md", "**/*.yaml", "**/*.html"]
alwaysApply: true
---

# StudyBuddyAI — 项目约束

## 项目入口

- 先读 `AGENTS.md`，再读 `docs/PROJECT_MAP.md`，理解全局架构后再动手。
- 语音交互架构决策见 `docs/VOICE_ARCH.md`（选型原因、协议规范、升级路径）。

## 架构红线

1. **状态机是核心骨架**：7 个状态、10 个事件、9 条转换规则。增删状态/事件/转换必须用户确认。
2. **AIProvider 是抽象边界**：所有 AI 调用必须通过 `ai/base.py:AIProvider` 接口，禁止在 orchestrator 里直接调用具体 API。唯一实现是 `ai/volcano_provider.py`（火山引擎 RTC 对话式 AI，ASR+LLM+TTS 三段式），禁止引入其他 AI SDK。
3. **Prompt 是品味编码**：`prompts/*.md` 的任何修改必须用户确认。不得在 Prompt 中包含帮孩子做作业或给答案的指令。
4. **Disk Is State**：会话状态持久化到 `data/sessions/`，报告到 `data/reports/`。不删除 `data/` 下的文件。
5. **配置与代码分离**：API Key 从环境变量读取，不硬编码。时间参数走 `config/settings.py`。

## 模块依赖方向（单向，不可逆）

```
server.py → orchestrator/ → ai/
                           → planner/
                           → monitor/
                           → reporter/
                           → prompts/
```

禁止 `ai/` 反向依赖 `orchestrator/`，禁止 `planner/` 依赖 `ai/`。

## 编码风格

- Python ≥ 3.11，使用 `from __future__ import annotations`
- 公开函数必须有类型注解
- 使用 `structlog` 而非 `print` 或 `logging`
- 单文件不超过 500 行，超过则拆分
- 异步函数用 `async def`，回调类型用 `Callable[..., Awaitable[...]]`

## 修改前检查

- 改状态机 → 先看 `orchestrator/state_machine.py` 中 `TRANSITIONS` 表
- 改 Prompt → 先看 `prompts/` 目录下对应文件 + `engine.py` 中如何加载
- 改 AI Provider → 先看 `ai/base.py` 接口定义 + `docs/VOICE_ARCH.md` 协议规范
- 改编排逻辑 → 先看 `engine.py` 中 `_on_state_change` 的 handler 字典

## 测试

- 修改核心逻辑后运行 `python scripts/lint_invariants.py` 检查不变量
- 确保新代码不破坏现有不变量
